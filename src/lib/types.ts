export type WebsiteContent = {
  niche: string;
  productInfo: string;
  rearrangedContent: string;
  suggestions: string[];
  selectedHeadline: string;
  finalContent: string;
  theme: string;
  layout: string;
};
